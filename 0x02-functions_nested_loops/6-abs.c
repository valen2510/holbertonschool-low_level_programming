#include "holberton.h"
/**
 * _abs - absolute of an integer
 * @z: - entry value integer
 * Return: absolute value of z
 */
int _abs(int z)
{

	if (z < 0)
	{
		z = z * (-1);
	}
	else
	{
	}

	return (z);
}
